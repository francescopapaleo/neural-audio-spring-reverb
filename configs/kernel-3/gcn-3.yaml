name: GCN
model_type: GCN

criterion1: stft
pre_emphasis: null
criterion2: smooth

optimizer: Adam
lr: 0.01
lr_scheduler: ReduceLROnPlateau
lr_patience: 10

max_epochs: 500
early_stop_patience: 50
current_epoch: 0

min_valid_loss: null

cond_dim: 2
c0 : 0.0
c1 : 0.0

in_ch: 1
out_ch: 1
n_channels: 32
n_blocks: 4
dilation_growth: 512
kernel_size: 3

