name: gcn-2500-film-nocollate-contrast-100
model_type: GCN_FiLM

criterion1: stft
pre_emphasis: null
criterion2: smooth

dataset: egfxset
sample_rate: 48000
bit_rate: 24

optimizer: Adam
lr: 0.001
lr_scheduler: ReduceLROnPlateau
lr_patience: 20

batch_size: 8
num_workers: 8

max_epochs: 1000
early_stop_patience: 100
current_epoch: 0

min_valid_loss: null

cond_dim: 2
c0 : 0.0
c1 : 0.0

kernel_size: 41
num_blocks: 2
num_channels: 32
dilation_depth: 6
num_layers: 5
num_repeat: 0
input_size: 1
output_size: 1
hidden_size: 0

